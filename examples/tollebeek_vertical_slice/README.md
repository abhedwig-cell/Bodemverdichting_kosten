# Tollebeek vertical-slice examples

No scientific input dataset is committed here yet.

The first committed CSV/JSON example should either:

- be a clearly labelled synthetic software fixture; or
- be a versioned, provenance-linked project dataset.

Do not create a plausible-looking Tollebeek example by filling unknown routing,
assist, head, efficiency or current soil-state values with defaults.
