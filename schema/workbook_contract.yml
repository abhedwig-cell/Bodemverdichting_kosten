contract_version: 0.2.0
status: STATUS_A_LIGHT_WORKING_BASELINE

workbook:
  required_front_matter:
    - sheet: 00_METADATA
      role: METADATA
    - sheet: 01_GUIDE
      role: GUIDE
    - sheet: DATA_DICTIONARY
      role: ARTIFACT_METADATA

  metadata_fields:
    required:
      - project_id
      - artifact_id
      - artifact_class
      - artifact_version
      - schema_version
      - generated_on
      - git_commit
      - purpose
      - scope
      - canonical_status
    recommended:
      - framework_version
      - intended_audience
      - input_datasets
      - evidence_snapshot
      - known_limitations
      - owner
      - contact

  dataset_metadata:
    required:
      - dataset_id
      - entity
      - role
      - grain
      - primary_key
      - canonical_status
      - source_or_derivation
    recommended:
      - dataset_version
      - purpose
      - qualification_status
      - known_limitations

  field_help:
    canonical_source: merged canonical field registry
    preferred_excel_surface:
      - header_note_or_comment
      - data_validation_input_message
    fallback: DATA_DICTIONARY
    max_header_label_length_recommendation: 40

  principles:
    - one_table_one_dataset
    - unknown_is_null_not_zero
    - views_are_not_source_tables
    - artifact_version_does_not_imply_cumulative_inheritance
    - provenance_and_qualification_must_survive_into_artifacts
    - scenario_context_must_be_explicit_when_it_changes_interpretation
